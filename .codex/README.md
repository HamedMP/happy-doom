# Codex Workspace Assets

Use this directory for repo-local AI development assets.

## Future Additions

- `skills/` for local Codex skills.
- `plugins/` for local Codex plugin experiments.

Keep each asset self-contained and documented so agents can discover and use it safely.
